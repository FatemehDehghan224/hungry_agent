# DFS Agent (Simple AI Project)

Simple DFS project with 3 modes:
- Text mode (read grid from file)
- Random mode (random environment + text output)
- Graphic mode (pygame visualization)

## Requirements
```bash
pip install -r requirements.txt
